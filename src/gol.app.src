{application, gol,
 [{description, "Erlang Game of Life"},
  {vsn, "0.0.1"},
  {registered, [main_indexer, cache, main_persister] },
  {mod, {gol, ["input.txt"]}},
  {modules,
   [automaton_sup,
    row_sup,
    cell,
    cache,
    register_sup,
    register_row_sup,
    main_indexer,
    row_indexer
  ]}
 ]}.
